# Requirements for Diffusion LLM Safety Evaluation

# IMPORTANT: LLaDA and MMaDA require different transformers versions!
#
# For LLaDA inference:
#   pip install -r src/requirements_llada.txt
#
# For MMaDA inference:
#   pip install -r src/requirements_mmada.txt
#
# Switch between them as needed when running evaluations sequentially.

# Common dependencies (install with either requirements file)
torch>=2.0.0
accelerate>=0.24.0
tqdm>=4.65.0
pytest>=7.4.0

# Note: transformers version differs - see requirements_llada.txt or requirements_mmada.txt


# Optional: For better performance
ninja  # Fast compilation for certain ops

jaxtyping
